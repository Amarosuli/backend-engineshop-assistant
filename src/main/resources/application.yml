spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/engineshop
    username: postgres
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
project:
  poster: poster/
